import React from 'react';
import { Register } from '@widgets/forms';

import { SBaseContainer } from './registration.styled';

export const Registration = () => {
  return (
    <SBaseContainer>
      <Register />
    </SBaseContainer>
  );
};
