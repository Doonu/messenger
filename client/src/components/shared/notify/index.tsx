import React from 'react';
import { SContainer, SNotify } from './Notify.styled';

const Notify = () => {
  return (
    <SContainer>
      <SNotify />
    </SContainer>
  );
};

export default Notify;
